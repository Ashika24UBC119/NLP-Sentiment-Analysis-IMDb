# Dataset

This project uses the IMDb Large Movie Review Dataset.

The dataset contains 50,000 movie reviews labeled as positive or negative.

The original dataset is available from the IMDb Large Movie Review Dataset source.

Due to GitHub's file size limit, the complete CSV dataset is not uploaded to this repository.

To run the project, download the dataset and place `IMDB Dataset.csv` inside this `dataset` folder.
